#import "lib/template.typ": template, latin
#import "lib/utilities.typ": ltr-footnote, en
#import "lib/theorems.typ": definition, theorem, observation, cproof, reset-theorem-counter
#import "lib/algorithms.typ": algorithm

#show: template.with(
  title: "پوشش دیسک واحد",
  course: "نام درس",
  author: "نام و نام خانوادگی",
  organization: "دانشگاه صنعتی امیرکبیر، دانشکده مهندسی کامپیوتر",
  date: "خردادماه 1401",
)

#set heading(numbering: "1")
#set figure(numbering: "1")
#show heading.where(level: 1): it => {
  reset-theorem-counter()
  it
}

*چکیده*

در این گزارش، چهار مورد از الگوریتم‌های تقریبی که اخیراً برای مسئله پوشش دیسک واحد ارائه شده‌اند شرح داده می‌شوند و پس از پیاده‌سازی، با استفاده از چند مجموعه‌نقطه دنیای واقعی، مورد ارزیابی تجربی قرار می‌گیرند. معیارهای ارزیابی، تعداد دیسک‌های درنظر گرفته شده و زمان اجرای هر الگوریتم خواهد بود. در نهایت عملکرد هر الگوریتم و بهترین الگوریتم‌ها برای هر معیار گزارش می‌شود.

= شرح مسئله

مجموعه $P$ شامل $n$ نقطه در صفحه داده می‌شود. هدف، پوشش تمام نقاط با استفاده از کمترین تعداد دیسک با شعاع واحد ($r=1$) است. این مسئله، پوشش دیسک واحد (UDC)#ltr-footnote[Unit Disk Cover] نامیده شده و یک مسئله ان‌پی‌سخت#ltr-footnote[NP-hard] به‌حساب می‌آید @fowler1981optimal. در کاربردهای مختلف مانند شبکه‌های بی‌سیم، تعیین موقعیت، برنامه‌ریزی حرکت، پردازش تصاویر و... استفاده می‌شود.

= مرور الگوریتم‌ها

از سال 1991 تاکنون الگوریتم‌های زیادی ارائه شده‌اند که این مسئله را به‌صورت تقریبی با فاکتورهای تقریب و پیچیدگی‌های زمانی متفاوت، در نُرم اقلیدسی حل می‌کنند. @tb1 تاریخچه الگوریتم‌های تقریبی ارائه شده برای این مسئله را نشان می‌دهد.

#figure(
  kind: table,
  caption: [خلاصه الگوریتم‌های تقریبی ارائه شده برای UDC],
  table(
    columns: 4,
    stroke: .4pt,
    align: center,
    [مرجع], [فاکتور تقریب], [پیچیدگی زمانی], [سال],
    [@gonzalez1991covering], [$2(1+1/l)$], [$O(l^2 n^7)$], [1991],
    [@gonzalez1991covering], [$8$], [$O(n log S)$], [1991],
    [@bronnimann1995almost], [$O(1)$], [$O(n^3 log n)$], [1995],
    [@franceschetti2001geometric], [$3(1+1/l)^2$], [$O(K n)$], [2001],
    [@fu2007almost], [$2.8334$], [$O(n (log n log log n)^2)$], [2007],
    [@liu2014fast], [$25/6$], [$O(n log n)$], [2014],
    [@biniaz2017approximation], [$4$], [$O(n log n)$], [2017],
    [@imanparast2020simple], [$4$], [$O(n log n)$], [2018],
    [@dumitrescu2020online], [$O(1.321^d)$], [$-$], [2018],
    [@ghosh2019unit], [$7$], [$O(n)$], [2019],
  ),
) <tb1>

در ادامه، به‌ترتیب الگوریتم‌های ذیل بررسی می‌شوند:

- الگوریتم BLMS که در سال 2017 ارائه شده است @biniaz2017approximation.
- الگوریتم LL که در سال 2014 ارائه شده است @liu2014fast.
- الگوریتم DGT که در سال 2018 ارائه شده است @dumitrescu2020online.
- الگوریتم FastCover که در سال 2019 ارائه شده است @ghosh2019unit.

عنوان سه الگوریتم‌ اول، برگرفته از حرف اول نام پژوهشگران مربوط به آن است.

== الگوریتم BLMS

مجموعه $P$ را به‌عنوان مجموعه نقاط ورودی که در صفحه قرار دارند و $C^*$ را پوشش دیسک بهینه برای آن در نظر بگیرید. به‌خاطر داشته باشید که شعاع هر دیسک واحد برابر با 1 است.

#definition[
در گراف تقاطع دیسک واحد#ltr-footnote[Unit Disk Intersection Graph] که با UDIG(P) نمایش داده می‌شود، نقاط موجود در مجموعه $P$ رئوس را تشکیل می‌دهند و برای هر جفت $p, q$ در $P$ یک یال وجود دارد اگر و تنها اگر $|p q| <= 2$ باشد که $|p q|$ فاصله اقلیدسی بین $p$ و $q$ را نشان می‌دهد.
] <def1>

#observation[
برای دو نقطه $p, q$ در $P$، اگر $(p, q)$ عضوی از UDIG(P) نباشد، آنگاه $p$ و $q$ نمی‌توانند با یک دیسک واحد پوشش داده شوند.
] <ob1>

#definition[
یک مجموعه مستقل در UDIG(P)، زیرمجموعه‌ای مانند $I$ از مجموعه $P$ است، به‌طوری که هیچ یالی بین جفت نقطه‌های موجود در $I$ وجود ندارد. همچنین $I$ مجموعه مستقل حداکثری#ltr-footnote[Maximal Independent Set] نامیده می‌شود، اگر برای هر $p in P \ I$، مجموعه $I ∪ {p}$ در UDIG(P) مستقل نباشد.
]

فرض کنید $I$ مجموعه مستقل حداکثری در UDIG(P) باشد. طبق مشاهده بالا، اندازه هر مجموعه مستقل در UDIG(P) یک حد پایین#ltr-footnote[Lower Bound] برای تعداد دیسک‌های موردنیاز به‌منظور پوشش $P$ است. بنابراین خواهیم داشت:

$
|I| <= |C^*|
$ <eq1>

مطابق @figf1 برای پوشش یک دیسک با شعاع 2، هفت دیسک واحد با شعاع 1 لازم و کافی است. بر همین اساس، یک الگوریتم با فاکتور تقریب 7 برای مسئله UDC به‌دست می‌آید.

#figure(
  image("figs/f1.jpg", width: 40%),
  caption: [پوشش $D(p,2)$ با دیسک‌های واحد],
) <figf1>

در ادامه خواهید دید که چگونه می‌توان فاکتور تقریب را به 4 کاهش داد. $p$ را سمت چپ‌ترین نقطه در مجموعه $P$ درنظر بگیرید. در موارد خاص که مقادیر $x$ نقاط با هم برابر است، برای انتخاب سمت چپ‌ترین نقطه، کمتر بودن مقدار $y$ را ملاک قرار می‌دهیم. فرض کنید خط عمودی $l$ از نقطه $p$ عبور کند؛ $R(p)$ اشتراک $D(p,2)$ با نیم‌صفحه#ltr-footnote[Half-plane] سمت راست $l$ خواهد بود؛ یعنی $R(p)$ نیم‌دیسک سمت راست $D(p,2)$ است (مطابق @figf2).

#figure(
  image("figs/f2.jpg", width: 30%),
  caption: [پوشش $R(p)$ با دیسک‌های واحد],
) <figf2>

#algorithm(caption: [نسخه اولیه BLMS])[
  1. C = ∅
  2. I = ∅
  3. L = List of points in P sorted from left to right
  4. while L is not empty:
  5.   p = first element of L
  6.   if d(p, I) > 2:
  7.     Cover R(p) by 4 unit disks c1, c2, c3, c4
  8.     C = C ∪ {c1, c2, c3, c4}
  9.     I = I ∪ {p}
  10.  L = L - {p}
  11. return C
] <blms1>

در هر تکرار الگوریتم BLMS، نقطه $p$ به مجموعه $I$ اضافه می‌شود، اگر و تنها اگر $d(p,I) > 2$ باشد. بنابراین $p$ در UDIG(P) به هیچ نقطه‌ای از $I$ متصل نیست. همچنین بعد از خاتمه الگوریتم، $I$ یک مجموعه مستقل حداکثری خواهد بود.

#theorem[
فاکتور تقریب نسخه اولیه BLMS برای مسئله پوشش دیسک واحد، 4 است.
]

#cproof[
مجموعه نقاط $I$ و مجموعه دیسک‌های واحد $C$ را در پایان الگوریتم، درنظر بگیرید. براساس نامساوی بالا می‌دانیم که $|I| <= |C^*|$ برقرار است. چون برای هر نقطه $p in I$، نیم‌دیسک $R(p)$ با 4 دیسک واحد پوشش داده می‌شود، مجموعه $C$ مجموعه $P$ را پوشش می‌دهد. بنابراین رابطه $|C| <= 4|I| <= 4|C^*|$ برقرار است.
]

پیچیدگی زمانی نسخه اولیه BLMS برابر $O(n log n + n · t(d))$ است. همچنین محاسبه فاصله با پیچیدگی زمانی $O(log^2 n)$ قابل انجام است @bentley1980decomposable.

#algorithm(caption: [نسخه بهبود یافته BLMS با تکنیک جاروی صفحه])[
  1. Initialize event queue Q using ascending x-order of points.
  2. Initialize empty BST status T.
  3. Initialize empty list C.
  4. while Q is not empty:
  5.   get next event point p and delete it.
  6.   if p is an end-point: delete corresponding start-point from T.
  7.   else:
  8.     find top-2 and bottom-2 neighbors of p in T.
  9.     if distance to all four neighbors is greater than 2:
  10.      compute 4 unit-disk centers, append to C.
  11.      insert p into T.
  12.      insert q = (p_x + 2, p_y) into Q.
  13. return C
] <blms2>

== الگوریتم LL

در این الگوریتم، صفحه به نوار‌های عمودی با عرض $sqrt(3)$ تقسیم می‌شود. از هر نوار، یک جواب تقریبی با مرتب‌سازی نقاط براساس مؤلفه $y$ به‌صورت نزولی به‌دست می‌آید. نقطه بعدی درون یک نوار که هنوز پوشش داده نشده است، با قرار دادن یک دیسک در پایین‌ترین مکان ممکن، پوشش داده می‌شود. مرکز این دیسک‌ها، روی خطوط عمودی که نوارها را به دو قسمت تقسیم می‌کنند قرار می‌گیرد. جواب نهایی با اجتماع جواب همه نوارها حاصل می‌شود. این سامانه نواری، 5 مرتبه به سمت راست و هر دفعه به‌اندازه $sqrt(3)/6$ شیفت داده می‌شود. در هر شیف، یک جواب به‌دست می‌آید. از بین این 6 جواب، آن جوابی که کمترین دیسک را استفاده کرده باشد به‌عنوان جواب نهایی درنظر گرفته می‌شود. جزئیات بیشتر در الگوریتم زیر آمده است. فاکتور تقریب این الگوریتم $25/6 ~= 4.17$ است.

#algorithm(caption: [محاسبه موقعیت دیسک‌های واحد با استفاده از LL])[
  1. Disk-Centers ← ∅, min ← n+1
  2. Sort P w.r.t x-coordinate in O(n log n)
  3. for i in {0,1,2,3,4,5}:
  4.   current ← 1, C ← ∅, right ← P_1x + i*sqrt(3)/6
  5.   while current ≤ n:
  6.     index ← current
  7.     while P_current_x < right and current ≤ n:
  8.       current ← current + 1
  9.     x-of-restriction-line ← right - sqrt(3)/2
  10.    segments ← ∅
  11.    for j from index to current-1:
  12.      d ← P_jx - x-of-restriction-line
  13.      y ← sqrt(1-d^2)
  14.      create segment s on restriction line and insert into segments
  15.    sort segments by non-ascending top y
  16.    greedily stab segments from top to bottom and add stabbing points to C
  17.    increment right by a multiple of sqrt(3) such that P_current - right ≤ sqrt(3)
  18.  if |C| < min:
  19.    Disk-Centers ← C, min ← |C|
  20. return Disk-Centers
] <algLL>

== الگوریتم DGT

این الگوریتم، ساده و برخط است. به‌ازای هرنقطه‌ای که تاکنون تحت پوشش قرار نگرفته است، یک دیسک واحد به مرکز آن نقطه ایجاد می‌شود. فاکتور تقریب آن در صفحه، 5 است. در فضای با ابعاد $d$ دارای فاکتور تقریب $O(1.321^d)$ است. برای مشاهده توصیف سطح بالا، به الگوریتم زیر مراجعه نمایید.

#algorithm(caption: [محاسبه موقعیت دیسک‌های واحد با استفاده از DGT])[
  1. Disk-Centers ← ∅
  2. For p ∈ P:
  3.   if distance to nearest point in Disk-Centers is > 1:
  4.     Disk-Centers ← Disk-Centers ∪ {p}
  5. Return Disk-Centers
] <algDGT>

== الگوریتم FastCover

در این الگوریتم، از یک شبکه#ltr-footnote[Grid] با مربع‌های به ضلع $sqrt(2)$ استفاده می‌شود. هر مربع از این شبکه، می‌تواند توسط یک دیسک به شعاع واحد محاط شود. به‌ازای هر نقطه، اگر توسط یکی از دیسک‌هایی که قبلاً قرار گرفته است تحت پوشش باشد، عملی انجام نمی‌شود؛ در غیر این صورت، یک دیسک واحد به مرکز مربعی که آن نقطه درونش قرار گرفته است، ایجاد می‌شود.

در پیاده‌سازی برای جستجوی سریع‌تر، از یک جدول درهم‌ساز#ltr-footnote[Hash table] استفاده می‌شود. در این جدول، مختصات مرکز دیسک‌هایی که اضافه شده‌اند ذخیره می‌شود. برای جلوگیری از مشکلات اعداد اعشاری، از یک جفت عدد صحیح برای نمایش مرکز هر دیسک استفاده می‌شود. مختصات حقیقی می‌تواند با ضرب کردن هر عدد صحیح در $sqrt(2)$ و اضافه کردن $1/sqrt(2)$ به آن به‌دست بیاید. برای به‌دست آوردن اعداد صحیح از روی یک نقطه، مؤلفه‌های $x,y$ آن بر $sqrt(2)$ تقسیم می‌شود. این فرآیند در الگوریتم زیر قابل ملاحظه است.

این الگوریتم دارای فاکتور تقریب 7 و پیچیدگی زمانی $O(n)$ است. یک الگوریتم برخط به‌حساب می‌آید و هیچ پیش‌پردازشی مثل مرتب‌سازی روی نقاط انجام نمی‌دهد. به‌اندازه $O(s)$ حافظه اضافی مصرف می‌کند که $s$ نشان‌دهنده اندازه پوشش تولید شده است.

#algorithm(caption: [محاسبه موقعیت دیسک‌های واحد با استفاده از FastCover])[
  1. H ← ∅, Disk-Centers ← ∅
  2. For p ∈ P:
  3.   i ← floor(p_x/sqrt(2)), j ← floor(p_y/sqrt(2))
  4.   if (i,j) ∉ H:
  5.     insert (i,j) into H and corresponding center into Disk-Centers
  6. Return Disk-Centers
] <algFast>

این الگوریتم را می‌توان کمی بهبود داد. وقتی که یک نقطه در یک مربع از شبکه قرار می‌گیرد، ممکن است توسط 4 دیسک واحد که مربوط به مربع‌های همسایه است و قبلاً اضافه شده‌اند تحت پوشش باشد (مطابق @figf3).

#figure(
  image("figs/f3.jpg", width: 40%),
  caption: [امکان پوشش یک نقطه با دیسک‌های مربع‌های همسایه],
) <figf3>

#algorithm(caption: [محاسبه موقعیت دیسک‌های واحد با استفاده از FastCover+])[
  1. H ← ∅, Disk-Centers ← ∅
  2. For p ∈ P:
  3.   i ← floor(p_x/sqrt(2)), j ← floor(p_y/sqrt(2))
  4.   if (i,j) in H:
  5.     continue
  6.   else if east/west/north/south neighbor grid-disk already exists and covers p:
  7.     continue
  8.   else:
  9.     insert (i,j) into H and corresponding center into Disk-Centers
  10. Return Disk-Centers
] <algFastPlus>

#v(0.2cm)
بهبود دیگری می‌توان روی الگوریتم اعمال نمود. اگر نقاط موجود در دو دیسک مجاور را بتوان با یک دیسک پوشش داد، یعنی قطر مجموعه نقاط هر دو دیسک کمتر از 2 باشد، می‌توان آن دو دیسک را ادغام نمود. برای ادغام، یک دیسک واحد به مرکز وسط قطر مجموعه نقاط درنظر گرفته می‌شود. برای اینکه محاسبه قطر، تأثیری روی پیچیدگی زمانی الگوریتم نداشته باشد، به‌همراه هر دیسک واحدی که ایجاد می‌شود، یک مستطیل مرزی نیز برای مجموعه نقاط موجود در آن نگهداری می‌شود. هر دفعه که یک نقطه جدید تحت پوشش یک دیسک واحد قرار می‌گیرد، ابعاد مستطیل مرزی مربوط به آن نیز بروزرسانی می‌شود. این بروزرسانی در $O(1)$ قابل انجام است.

#algorithm(caption: [محاسبه موقعیت دیسک‌های واحد با استفاده از FastCover++])[
  1. H ← ∅, Disk-Centers ← ∅
  2. For p ∈ P:
  3.   i ← floor(p_x/sqrt(2)), j ← floor(p_y/sqrt(2))
  4.   if (i,j) in H: update B(i,j) using p
  5.   else if one of four neighbor grid-disks covers p: update its bounding box
  6.   else: insert (i,j) into H and initialize B(i,j)
  7. While there is an unprocessed grid-disk (i,j) in H:
  8.   if there is a neighbor (k,l) in H with |i-k|≤1, |j-l|≤1 and
  9.      diagonal-length of B(i,j) ∪ B(k,l) ≤ 2:
  10.     remove both from H and add center of merged box to Disk-Centers
  11. For every remaining (i,j) in H:
  12.   add corresponding grid center to Disk-Centers
  13. Return Disk-Centers
] <algFastPP>

= ارزیابی

همه الگوریتم‌ها با زبان C++ و کتابخانه CGAL پیاده‌سازی شده‌اند. هرکدام از آنها بر روی 10 مجموعه‌نقطه دنیای واقعی اجرا شده‌اند. @tbeval نتایج ارزیابی را نشان می‌دهد. منظور از LL-1P، اجرای یک مرحله‌ای @algLL به‌جای شش مرحله است.

#figure(
  kind: table,
  caption: [نتایج ارزیابی الگوریتم‌ها],
  table(
    columns: 8,
    stroke: .4pt,
    align: center,
    [], [LL], [LL-1P], [BLMS], [DGT], [FastCover], [FastCover+], [FastCover++],
    [birch3], [99989, 0.18], [99991, 0.03], [99994, 0.05], [99993, 0.07], [99996, 0.02], [99995, 0.02], [99980, 0.08],
    [monalisa], [100000, 0.15], [100000, 0.03], [100000, 0.11], [100000, 0.08], [100000, 0.02], [100000, 0.02], [100000, 0.08],
    [usa], [115475, 0.17], [115475, 0.04], [115475, 0.12], [115475, 0.09], [115475, 0.02], [115475, 0.03], [115475, 0.10],
    [KDDCU2D], [1147, 0.19], [1152, 0.04], [1692, 0.10], [1626, 0.01], [1418, 0.01], [1374, 0.01], [1257, 0.01],
    [europe], [168253, 0.35], [168271, 0.06], [168088, 0.19], [168069, 0.16], [168333, 0.03], [168277, 0.04], [167811, 0.20],
    [wildfires], [622, 3.74], [622, 0.88], [842, 1.21], [787, 0.12], [663, 0.04], [637, 0.04], [620, 0.06],
    [world], [6667, 2.84], [6680, 0.51], [9145, 0.79], [10980, 0.15], [7874, 0.03], [7576, 0.04], [6967, 0.07],
    [nyctaxi], [25, 13.84], [26, 3.09], [32, 2.90], [31, 0.13], [34, 0.05], [31, 0.05], [25, 0.10],
    [uber], [3, 21.06], [3, 4.75], [5, 4.03], [5, 0.19], [5, 0.06], [4, 0.06], [4, 0.16],
    [hail2015], [888, 39.83], [889, 9.82], [1193, 11.19], [1128, 0.74], [901, 0.28], [860, 0.28], [847, 0.42],
  ),
) <tbeval>

= نتیجه‌گیری

در مجموعه‌نقطه‌های دنیای واقعی، اگر معیار با اهمیت‌تر برای ارزیابی، تعداد دیسک‌های استفاده شده باشد، الگوریتم LL عملکرد بهتری دارد. هرچند که الگوریتم FastCover++ در برخی موارد، هم از لحاظ تعداد دیسک‌ها و هم زمان اجرا، عملکرد بهتری داشته است.

اگر معیار مهم‌تر، زمان اجرا باشد الگوریتم FastCover عملکرد بهتری داشته است؛ اما چون تفاوت چندانی با نسخه بهبودیافته خود که تعداد دیسک‌های کمتری تولید می‌کند ندارد، استفاده از FastCover++ پیشنهاد می‌شود.

اگر هردو معیار تعداد دیسک‌ها و زمان اجرا با اهمیت باشد، استفاده از الگوریتم FastCover++ توصیه می‌شود؛ چرا که تعادل خوبی بین هردو معیار برقرار می‌کند @friederich2022experiments.

#pagebreak()

#heading(level: 1, numbering: none)[مراجع]
#text(lang: "en", dir: ltr)[
  #bibliography("etc/references.bib", style: "ieee")
]

#pagebreak()

#heading(level: 1, numbering: none)[پیوست]

همان‌طور که در @tb1 ملاحظه شد، بهترین الگوریتم‌هایی که تاکنون برای مسئله UDC ارائه شده‌اند، دارای فاکتور تقریب 4 هستند. اینجانب، توجه زیادی به این مسئله با هدف بهبود فاکتور تقریب و نگارش مقاله نمودم. ایده‌های مختلفی برای بهبود فاکتور تقریب به ذهنم رسید که پس از بررسی‌های فراوان متوجه شدم برخی از آنها اشتباه است و برخی دیگر را به‌دلیل کمبود زمان نتوانستم به‌طور دقیق بررسی کنم.

ایده اول، با هدف کاهش فاکتور تقریب از 4 به 3 بود. در @figf2 نشان داده شد که برای پوشش یک نیم‌دیسک به شعاع 2، دقیقاً به 4 دیسک واحد احتیاج است. پس از بررسی مشخص شد که این ایده عملی نیست، چون فرض اولیه اشتباه است و حالت‌هایی وجود دارد که اشتراک دو نیم‌دیسک بیشتر از حد تصور می‌شود و نمی‌توان آن را با یک ربع‌دیسک پوشش داد (مطابق @figapp1).

#figure(
  image("figs/app1.png", width: 30%),
  caption: [عدم امکان پوشش اشتراک دو نیم‌دیسک با ربع‌دیسک],
) <figapp1>

ایده دوم نیز با هدف کاهش فاکتور تقریب از 4 به 3 بود. به‌جای نیم‌دیسک‌های با شعاع 2، ربع‌دیسک‌هایی به شعاع 2 درنظر گرفته می‌شود که هرکدام با 3 دیسک واحد قابل پوشش هستند.

#pagebreak()

#algorithm(caption: [ایده دوم برای کاهش فاکتور تقریب به 3])[
  1. Initialize event queue Q in descending y-order.
  2. Initialize empty BST T and list C.
  3. while Q is not empty:
  4.   process start/end events of quarter-disks.
  5.   if left neighbors are farther than 2:
  6.     add 3 covering unit disks.
  7. return C
] <algapp1>

پس از بررسی، مشخص شد که این ایده نیز عملی نیست. زیرا مانند @figapp2 حالت‌هایی وجود دارد که ربع‌دیسک‌های زیادی با یکدیگر همپوشانی پیدا می‌کنند و در نهایت فاکتور تقریب، $O(n)$ می‌شود.

#figure(
  image("figs/app2.png", width: 25%),
  caption: [حالتی که ربع‌دیسک‌های زیادی همپوشانی پیدا می‌کنند],
) <figapp2>
